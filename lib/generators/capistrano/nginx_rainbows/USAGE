To create local nginx and rainbows configuration files call

    rails generate capistrano:nginx_rainbows:config [path]

The default path is "config/deploy/templates". You can override it like so:

    rails generate capistrano:nginx_rainbows:config "config/templates"

If you override templates path, don't forget to set "templates_path" variable in your deploy.rb
